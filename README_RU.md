# 🎮 Epic Free Games Claimer

Автоматическое получение бесплатных еженедельных игр из [Epic Games Store](https://store.epicgames.com/free-games). Больше не пропустите ни одной бесплатной игры!

[🌍 Другие языки](README.md)

## Возможности

- 📋 **Список** текущих и предстоящих бесплатных игр (без авторизации)
- 🤖 **Автоматическое получение** через автоматизацию браузера
- 🔐 **Один вход** — сессии сохраняются между запусками
- 👥 **Мульти-аккаунт** — настройка через `data/config.json`
- 🔔 **Уведомления** — Webhook (Telegram, Discord и др.)
- ⏰ **Планирование** — Cron / GitHub Actions
- 🐳 **Поддержка Docker**

## Быстрый старт

```bash
git clone https://github.com/bigu1/epic-free-games.git
cd epic-free-games
bash scripts/setup.sh
```

Ручная настройка:

```bash
npm install
npx playwright install firefox
node src/index.js --login    # Войти в Epic Games
node src/index.js --claim    # Получить бесплатные игры!
```

## Использование

```bash
# Показать бесплатные игры этой недели
node src/index.js --list

# Войти в Epic Games
node src/index.js --login

# Получить все бесплатные игры
node src/index.js --claim

# Тестовый запуск (без реального получения)
DRYRUN=1 node src/index.js --claim

# Проверить статус
node src/index.js --status
```

## Настройка

Скопировать `.env.example` в `.env`:

```bash
cp .env.example .env
```

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `EG_EMAIL` | Нет | Email аккаунта Epic Games |
| `EG_PASSWORD` | Нет | Пароль Epic Games |
| `EG_OTPKEY` | Нет | TOTP-секрет для 2FA |
| `HEADLESS` | Нет | `0` — показать браузер, `1` — фоновый режим |
| `WEBHOOK_URL` | Нет | URL Webhook для уведомлений |
| `DRYRUN` | Нет | `1` — пропустить реальные покупки |

## Планирование

### Cron (Linux / macOS)

Epic обновляет бесплатные игры каждый четверг:

```bash
# Каждый четверг в 00:30
30 0 * * 4 cd /path/to/epic-free-games && node src/index.js --claim >> /tmp/epic-free-games.log 2>&1
```

## Docker

```bash
docker compose build
docker compose run epic-free-games node src/index.js --login
docker compose up
```

## Мульти-аккаунт

Создать `data/config.json`:

```json
{
  "accounts": [
    { "email": "user1@example.com", "password": "pass1", "otpkey": "" },
    { "email": "user2@example.com", "password": "pass2", "otpkey": "SECRET" }
  ]
}
```

## Лицензия

[MIT](LICENSE)

## Отказ от ответственности

Этот инструмент автоматизирует взаимодействие браузера с Epic Games Store. Используйте на свой страх и риск. Проект не связан с Epic Games, Inc. и не одобрен ею.

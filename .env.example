# Epic Games Store credentials (optional — you can also login interactively)
# For single account, use env vars:
# EG_EMAIL=your@email.com
# EG_PASSWORD=your_password

# 2FA TOTP secret key (optional — for automatic 2FA code generation)
# EG_OTPKEY=JBSWY3DPEHPK3PXP

# Parental control PIN (if enabled on your Epic account)
# EG_PARENTALPIN=1234

# For multiple accounts, create data/config.json instead:
# {
#   "accounts": [
#     { "email": "user1@example.com", "password": "pass1", "otpkey": "" },
#     { "email": "user2@example.com", "password": "pass2", "otpkey": "TOTP_SECRET" }
#   ]
# }

# Browser settings
# HEADLESS=1          # Set to 0 to show browser window
# BROWSER_DIR=data/browser-profile  # Persistent browser data directory
# WIDTH=1280
# HEIGHT=720
# TIMEOUT=30000       # General timeout (ms)
# LOGIN_TIMEOUT=180000  # Login timeout (ms, default 3 min)

# Locale / Country (affects which free games are available)
# LOCALE=en-US
# COUNTRY=US

# Notification webhook URL (optional — receives JSON POST with { text, message })
# WEBHOOK_URL=https://your-webhook-url

# Debug mode
# DEBUG=0
# DRYRUN=0            # Set to 1 to skip actual purchases
